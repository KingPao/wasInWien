# Aktuell wird keine .env mehr benötigt.
# Reddit läuft über den öffentlichen JSON-Endpoint (kein Account nötig).
# Telegram läuft über die öffentliche Web-Vorschau t.me/s/<channel> (kein Account nötig,
# funktioniert nur für Channels mit öffentlichem Usernamen).
# Instagram ist ohne Login nicht zuverlässig scrapbar und bleibt deaktiviert
# (si